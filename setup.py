from setuptools import setup, find_packages

setup(
    name='pykospacing-lite',
    version='0.1',
    description='Lite version of PyKoSpacing without TensorFlow dependency',
    author='BS-patch',
    packages=find_packages(),
    python_requires='>=3.6',
)
